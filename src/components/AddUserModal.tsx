import React from "react";

type Props = {};

const AddUserModal = (props: Props) => {
  return (
    <div className="input absolute left-0 top-14 bg-[#454545] z-30 min-w-full">
      <button onClick={() => {}}>Add User</button>
    </div>
  );
};

export default AddUserModal;
